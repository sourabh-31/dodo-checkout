{
  "name": "@dodo/checkout-sdk",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc && pnpm build:script",
    "build:script": "esbuild src/global.ts --bundle --format=iife --minify --sourcemap --outfile=dist/dodo-checkout.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
